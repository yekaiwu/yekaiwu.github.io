# The portfolio is rendered by the custom home template in layouts/index.html.
# Keep site content and metadata close to that implementation rather than
# inheriting settings from the starter theme this site replaced.
